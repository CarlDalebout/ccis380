%-*-latex-*-
\proof
By definition,
$\vec{u} = \langle a, b \rangle$,
$\vec{v} = \langle c, d \rangle$,
$\vec{w} = \langle e, f \rangle$
for some $a, b, c, d, e, f \in \R$.

(a)
We have
\begin{align*}
  \vec{u} \cdot \vec{v}
  &= \langle a, b, \rangle \cdot \langle c, d \rangle \\
  &=    \\
  &=    \\
\end{align*}

(b)

(c)

(d)

(e)

(f)

(g)
\qed

