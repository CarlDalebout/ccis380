#version 330 core

in vec3 vpos;

out vec4 fcolor;

void main()
{
    fcolor = vec3(0, 0, 0, 1);

   /*
   // ambient
   vec3 ambient = vec3(0, 0, 0);

   // diffuse
   vec3 diffuse = vec3(0, 0, 0);

   // specular
   vec3 specular = vec3(0, 0, 0);
 
   vec3 result = computation involving ambient, diffuse, specular;
   fcolor = vec4(result, 1.0);
   */
}
